import React, { useEffect, useRef, useState } from 'react'
import { createRoot } from 'react-dom/client'
import { BoxSelect, ClipboardPaste, Copy, Eraser, MousePointer2, Plus, Save, FilePlus2, Upload, Download, ZoomIn, ZoomOut, RotateCcw, RotateCw, Trash2, WandSparkles, Crosshair, Square, Circle, DoorOpen, Box, FileJson } from 'lucide-react'
import './styles.css'

const key = (x, y) => `${x},${y}`
const CATEGORIES = ['terrain', 'ground', 'trees', 'objects', 'animated']
const categoryForName = name => { const n = name.toLowerCase(); return n.includes('tree') || n.includes('wood') ? 'trees' : n.includes('anim') || n.includes('walk') || n.includes('run') ? 'animated' : n.includes('ground') || n.includes('floor') ? 'ground' : n.includes('object') || n.includes('prop') || n.includes('item') ? 'objects' : 'terrain' }
const OBJECT_TYPES = ['platform', 'prop', 'pickup', 'enemy', 'exit']
const newLevelDoc = (id, name = id) => ({ metadata: { id, name, width: 24, backgroundSet: 'default' }, platforms: [], props: [], pickups: [], enemies: [], exits: [] })
const objectBucket = type => type === 'platform' ? 'platforms' : type === 'prop' ? 'props' : type === 'pickup' ? 'pickups' : type === 'enemy' ? 'enemies' : 'exits'

function App() {
  const canvasRef = useRef(null), dbRef = useRef(null), assetsRef = useRef([])
  const placedRef = useRef(new Map()), collisionsRef = useRef(new Set()), historyRef = useRef([]), redoRef = useRef([]), levelsRef = useRef({ main: null, gym: null })
  const paintingRef = useRef(false), lastPaintRef = useRef(''), buttonRef = useRef(0)
  const [assets, setAssets] = useState([]), [selected, setSelected] = useState(null), [selectedRegion, setSelectedRegion] = useState({ x: 0, y: 0, w: 32, h: 32 }), [pickerScale, setPickerScale] = useState(1), [modalPickerScale, setModalPickerScale] = useState(1), [pickerOpen, setPickerOpen] = useState(false), [zoom, setZoom] = useState(1), [pickerDragStart, setPickerDragStart] = useState(null), [level, setLevel] = useState('main')
  const [sourceFile, setSourceFile] = useState(null), [assetCategory, setAssetCategory] = useState('terrain'), [status, setStatus] = useState('Upload a tile to add it to your local library.')
  const [levelDocs, setLevelDocs] = useState(() => { try { const saved = JSON.parse(localStorage.getItem('pixel-pipeline-levels') || 'null'); return saved || { main: newLevelDoc('main', 'Main Level'), gym: newLevelDoc('gym', 'Gym') } } catch { return { main: newLevelDoc('main', 'Main Level'), gym: newLevelDoc('gym', 'Gym') } } })
  const [objectSelection, setObjectSelection] = useState(null), [objectMode, setObjectMode] = useState(false), [objectDrag, setObjectDrag] = useState(null)
  const [tileSize, setTileSize] = useState(32), [grid, setGrid] = useState(true), [collision, setCollision] = useState(false), [debug, setDebug] = useState(false)
  const [eraser, setEraser] = useState(false), [mode, setMode] = useState('brush'), [selection, setSelection] = useState(null), [selectionStart, setSelectionStart] = useState(null), [cursor, setCursor] = useState({ x: 0, y: 0 }), [lastTile, setLastTile] = useState({ x: 0, y: 0 }), [cursorActive, setCursorActive] = useState(false), [version, redraw] = useState(0)
  const clipboardRef = useRef([]), clipboardSizeRef = useRef({ w: 1, h: 1 })
  const bump = () => redraw(v => v + 1), t = Math.max(8, Number(tileSize) || 32)
  const currentDoc = levelDocs[level] || newLevelDoc(level)
  const levelWidth = Math.max(24, Number(currentDoc.metadata.width) || 24)
  const persistLevels = docs => { setLevelDocs(docs); localStorage.setItem('pixel-pipeline-levels', JSON.stringify(docs)) }
  const updateCurrentDoc = updater => persistLevels({ ...levelDocs, [level]: updater(currentDoc) })
  const addObject = type => { const id = 'obj-' + Date.now(); const base = type === 'platform' ? { assetId: selected?.name || '', x: lastTile.x * t, y: lastTile.y * t, scale: 1, collision: collision } : type === 'prop' ? { frameId: (selected?.name || '') + ':0,0', x: lastTile.x * t, y: lastTile.y * t, depth: 1 } : type === 'pickup' ? { pickupType: 'coin', x: lastTile.x * t, y: lastTile.y * t } : type === 'enemy' ? { enemyId: 'enemy', x: lastTile.x * t, y: lastTile.y * t, facing: 'right', tuning: {} } : { exitId: 'exit', x: lastTile.x * t, y: lastTile.y * t, collision: { w: t, h: t * 2 }, targetAnimation: 'fade' }; updateCurrentDoc(doc => ({ ...doc, [objectBucket(type)]: [...doc[objectBucket(type)], { id, ...base }] })); setObjectSelection({ type, id }); setObjectMode(true); setStatus('Added ' + type + '.') }
  const selectedObject = objectSelection ? (currentDoc[objectBucket(objectSelection.type)] || []).find(item => item.id === objectSelection.id) : null
  const updateSelectedObject = patch => { if (!selectedObject) return; updateCurrentDoc(doc => ({ ...doc, [objectBucket(objectSelection.type)]: doc[objectBucket(objectSelection.type)].map(item => item.id === selectedObject.id ? { ...item, ...patch } : item) })); bump() }
  const deleteSelectedObject = () => { if (!selectedObject) return; updateCurrentDoc(doc => ({ ...doc, [objectBucket(objectSelection.type)]: doc[objectBucket(objectSelection.type)].filter(item => item.id !== selectedObject.id) })); setObjectSelection(null); setStatus('Deleted object.') }

  const persist = asset => dbRef.current?.transaction('tiles', 'readwrite').objectStore('tiles').put({ name: asset.name, blob: asset.blob, category: asset.category })
  const removePersisted = name => dbRef.current?.transaction('tiles', 'readwrite').objectStore('tiles').delete(name)
  const select = asset => { setSelected(asset); setSelectedRegion({ x: 0, y: 0, w: t, h: t }); setEraser(false); setStatus(asset ? `Selected ${asset.name}` : 'No tile selected.') }
  const snapshot = () => ({ placed: new Map(placedRef.current), collisions: new Set(collisionsRef.current), assets: [...assetsRef.current], selected })
  const commit = () => { historyRef.current.push(snapshot()); redoRef.current = []; if (historyRef.current.length > 100) historyRef.current.shift() }
  const restore = s => { placedRef.current = new Map(s.placed); collisionsRef.current = new Set(s.collisions); assetsRef.current = [...s.assets]; setAssets(s.assets); select(s.selected); bump() }
  const undo = () => { const s = historyRef.current.pop(); if (!s) return; redoRef.current.push(snapshot()); restore(s); setStatus('Undid last action.') }
  const redo = () => { const s = redoRef.current.pop(); if (!s) return; historyRef.current.push(snapshot()); restore(s); setStatus('Redid last action.') }

  const addAsset = (blob, originalName = 'asset.png', save = true, category = assetCategory) => {
    const dot = originalName.lastIndexOf('.'), base = dot < 0 ? originalName : originalName.slice(0, dot), ext = dot < 0 ? '' : originalName.slice(dot)
    let name = originalName, n = 1; while (assetsRef.current.some(a => a.name === name)) name = `${base}-${n++}${ext}`
    const url = URL.createObjectURL(blob), image = new Image()
    image.onload = () => { const asset = { name, url, image, blob, category: category || categoryForName(name) }; assetsRef.current = [...assetsRef.current, asset]; setAssets(assetsRef.current); if (save) persist(asset); select(asset) }
    image.src = url
  }

  const switchLevel = next => { levelsRef.current[level] = { placed: new Map(placedRef.current), collisions: new Set(collisionsRef.current) }; let target = levelsRef.current[next]; if (!target) { const asset = selected || assetsRef.current[0], placed = new Map(), collisions = new Set(); for (let x = 1; x < 23; x++) { if (asset) placed.set(key(x, 16), { asset, sx: 0, sy: 0 }); collisions.add(key(x, 16)) } for (const [x, y] of [[4, 12], [5, 12], [6, 12], [11, 10], [12, 10], [13, 10], [18, 7], [19, 7]]) { if (asset) placed.set(key(x, y), { asset, sx: 0, sy: 0 }); collisions.add(key(x, y)) } target = { placed, collisions }; levelsRef.current[next] = target } placedRef.current = new Map(target.placed); collisionsRef.current = new Set(target.collisions); setLevel(next); setSelection(null); setCursor({ x: 0, y: 0 }); setLastTile({ x: 0, y: 0 }); setDebug(next === 'gym'); bump(); setStatus(next === 'gym' ? 'Gym sample platformer loaded. Debug overlay enabled.' : 'Main level loaded.') }

  useEffect(() => {
    const request = indexedDB.open('pixel-pipeline-assets', 1)
    request.onupgradeneeded = e => e.target.result.createObjectStore('tiles', { keyPath: 'name' })
    request.onsuccess = e => { dbRef.current = e.target.result; const q = e.target.result.transaction('tiles').objectStore('tiles').getAll(); q.onsuccess = () => q.result.forEach(item => addAsset(item.blob, item.name, false, item.category || categoryForName(item.name))) }
  }, [])

  useEffect(() => { const preview = document.querySelector('.selection img'); if (!preview || !selected) return; const c = document.createElement('canvas'); c.width = c.height = 62; const ctx = c.getContext('2d'); ctx.imageSmoothingEnabled = false; ctx.drawImage(selected.image, selectedRegion.x, selectedRegion.y, selectedRegion.w, selectedRegion.h, 0, 0, 62, 62); preview.src = c.toDataURL('image/png') }, [selected, selectedRegion, tileSize])

  useEffect(() => {
    const canvas = canvasRef.current, ctx = canvas?.getContext('2d'); if (!ctx) return
    ctx.imageSmoothingEnabled = false; ctx.fillStyle = '#0b1820'; ctx.fillRect(0, 0, canvas.width, canvas.height)
    for (const [k, placed] of placedRef.current) { const asset = placed.asset || placed, sx = placed.sx ?? 0, sy = placed.sy ?? 0, [x, y] = k.split(',').map(Number); ctx.drawImage(asset.image, sx, sy, t, t, x * t, y * t, t, t) }
    const drawAsset = (assetId, sx, sy, x, y, scale = 1) => { const asset = assetsRef.current.find(item => item.name === assetId); if (asset) ctx.drawImage(asset.image, sx, sy, t, t, x, y, t * scale, t * scale) }
    for (const item of currentDoc.platforms) { drawAsset(item.assetId, 0, 0, item.x, item.y, item.scale || 1); ctx.fillStyle = '#c6d2d6'; ctx.font = '10px monospace'; ctx.fillText(item.assetId || 'platform', item.x, item.y - 3); if (item.collision) { ctx.strokeStyle = 'rgba(231,106,77,.85)'; ctx.strokeRect(item.x, item.y, t * (item.scale || 1), t * (item.scale || 1)) } }
    for (const item of currentDoc.props) { const [assetId, coords = '0,0'] = (item.frameId || '').split(':'); const [sx = 0, sy = 0] = coords.split(',').map(Number); drawAsset(assetId, sx, sy, item.x, item.y); ctx.fillStyle = '#c6d2d6'; ctx.font = '10px monospace'; ctx.fillText(assetId || 'prop', item.x, item.y - 3) }
    for (const item of currentDoc.pickups) { ctx.fillStyle = '#efc75e'; ctx.beginPath(); ctx.arc(item.x + t / 2, item.y + t / 2, t * .22, 0, Math.PI * 2); ctx.fill() }
    for (const item of currentDoc.enemies) { ctx.fillStyle = '#e76a4d'; ctx.fillRect(item.x + t * .2, item.y + t * .2, t * .6, t * .6); ctx.fillStyle = '#e8e4d8'; ctx.font = '10px monospace'; ctx.fillText(item.enemyId, item.x, item.y - 3) }
    for (const item of currentDoc.exits) { ctx.strokeStyle = '#8ad1c2'; ctx.lineWidth = 2; ctx.strokeRect(item.x, item.y, item.collision?.w || t, item.collision?.h || t * 2); ctx.fillStyle = '#8ad1c2'; ctx.font = '10px monospace'; ctx.fillText(item.exitId, item.x, item.y - 3) }
    if (selectedObject) { const x = selectedObject.x, y = selectedObject.y; ctx.strokeStyle = '#efb36c'; ctx.lineWidth = 2; ctx.beginPath(); ctx.moveTo(x + t / 2, y + t / 2); ctx.lineTo(x + t * 2, y + t / 2); ctx.moveTo(x + t / 2, y + t / 2); ctx.lineTo(x + t / 2, y - t); ctx.stroke(); ctx.fillStyle = '#efb36c'; ctx.fillText('X', x + t * 2 + 4, y + t / 2); ctx.fillText('Y', x + t / 2 + 4, y - t - 4) }
    if (grid) { ctx.strokeStyle = '#213641'; ctx.lineWidth = 1; for (let x = 0; x <= canvas.width; x += t) { ctx.beginPath(); ctx.moveTo(x, 0); ctx.lineTo(x, canvas.height); ctx.stroke() } for (let y = 0; y <= canvas.height; y += t) { ctx.beginPath(); ctx.moveTo(0, y); ctx.lineTo(canvas.width, y); ctx.stroke() } }
    ctx.fillStyle = 'rgba(231,106,77,.4)'; for (const k of collisionsRef.current) { const [x, y] = k.split(',').map(Number); ctx.fillRect(x * t, y * t, t, t) }
    if (cursorActive) { ctx.strokeStyle = '#efb36c'; ctx.lineWidth = 2; ctx.strokeRect(cursor.x * t + 1, cursor.y * t + 1, t - 2, t - 2) }
    if (selection) { ctx.strokeStyle = '#f5c76e'; ctx.lineWidth = 2; ctx.setLineDash([6, 4]); ctx.strokeRect(selection.x * t + 1, selection.y * t + 1, selection.w * t - 2, selection.h * t - 2); ctx.setLineDash([]) }
    if (debug) { for (const k of placedRef.current.keys()) { const [x, y] = k.split(',').map(Number); ctx.strokeStyle = 'rgba(239,179,108,.35)'; ctx.strokeRect(x * t + 4, y * t + 4, t - 8, t - 8) } }
  }, [tileSize, grid, collision, eraser, cursor, cursorActive, selection, assets, selected, debug, mode, version, levelDocs, objectSelection])

  const cell = e => { const r = canvasRef.current.getBoundingClientRect(); return [Math.max(0, Math.min(levelWidth - 1, Math.floor((e.clientX - r.left) * canvasRef.current.width / r.width / t))), Math.max(0, Math.min(Math.floor(576 / t) - 1, Math.floor((e.clientY - r.top) * canvasRef.current.height / r.height / t)))] }
  const canvasPoint = e => { const r = canvasRef.current.getBoundingClientRect(); return { x: (e.clientX - r.left) * canvasRef.current.width / r.width, y: (e.clientY - r.top) * canvasRef.current.height / r.height } }
  const objectAt = point => { for (const type of [...OBJECT_TYPES].reverse()) for (const item of currentDoc[objectBucket(type)] || []) { const w = item.collision?.w || t, h = item.collision?.h || t; if (point.x >= item.x && point.x <= item.x + w && point.y >= item.y && point.y <= item.y + h) return { type, item } } return null }
  const objectPointerDown = e => { const point = canvasPoint(e), hit = objectAt(point); if (!hit) { setObjectSelection(null); return } setObjectSelection({ type: hit.type, id: hit.item.id }); setObjectDrag({ type: hit.type, id: hit.item.id, start: point, origin: { x: hit.item.x, y: hit.item.y }, axis: e.shiftKey ? 'x' : e.altKey ? 'y' : null }); e.currentTarget.setPointerCapture(e.pointerId) }
  const objectPointerMove = e => { if (!objectDrag) return; const point = canvasPoint(e), dx = point.x - objectDrag.start.x, dy = point.y - objectDrag.start.y, axis = objectDrag.axis || (Math.abs(dx) >= Math.abs(dy) ? 'x' : 'y'); updateSelectedObject({ x: Math.max(0, Math.round((objectDrag.origin.x + (axis === 'x' ? dx : 0)) / t) * t), y: Math.max(0, Math.round((objectDrag.origin.y + (axis === 'y' ? dy : 0)) / t) * t) }) }
  const objectPointerUp = e => { setObjectDrag(null); e.currentTarget.releasePointerCapture?.(e.pointerId) }
  const removeObjectAt = point => { const hit = objectAt(point); if (!hit) return false; updateCurrentDoc(doc => ({ ...doc, [objectBucket(hit.type)]: doc[objectBucket(hit.type)].filter(item => item.id !== hit.item.id) })); if (objectSelection?.id === hit.item.id) setObjectSelection(null); return true }
  const updateSelection = e => { const [x, y] = cell(e), s = selectionStart || { x, y }; setSelection({ x: Math.min(s.x, x), y: Math.min(s.y, y), w: Math.abs(x - s.x) + 1, h: Math.abs(y - s.y) + 1 }) }
  const resizeSelection = (dw, dh) => { if (!selection) return; setSelection(s => ({ ...s, w: Math.max(1, s.w + dw), h: Math.max(1, s.h + dh) })); setStatus('Level selection resized with Shift + arrows.') }
  const copySelection = () => { const area = selection || { x: lastTile.x, y: lastTile.y, w: 1, h: 1 }, value = placedRef.current.get(key(area.x, area.y)); if (!value && !selection) return setStatus('No tile at the keyboard cursor to copy.'); clipboardSizeRef.current = { w: area.w, h: area.h }; clipboardRef.current = []; for (let y = area.y; y < area.y + area.h; y++) for (let x = area.x; x < area.x + area.w; x++) { const v = placedRef.current.get(key(x, y)); if (v) clipboardRef.current.push({ x: x - area.x, y: y - area.y, value: v }) } setStatus(`Copied ${area.w}×${area.h} tile area.`) }
  const cutSelection = () => { const area = selection || { x: lastTile.x, y: lastTile.y, w: 1, h: 1 }, value = placedRef.current.get(key(area.x, area.y)); if (!value && !selection) return setStatus('No tile at the keyboard cursor to cut.'); commit(); clipboardSizeRef.current = { w: area.w, h: area.h }; clipboardRef.current = []; for (let y = area.y; y < area.y + area.h; y++) for (let x = area.x; x < area.x + area.w; x++) { const k = key(x, y), v = placedRef.current.get(k); if (v) clipboardRef.current.push({ x: x - area.x, y: y - area.y, value: v }); placedRef.current.delete(k); collisionsRef.current.delete(k) } bump(); setStatus(`Cut ${area.w}×${area.h} tile area.`) }
  const pasteSelection = () => { if (!clipboardRef.current.length) return setStatus('Nothing copied.'); const { w, h } = clipboardSizeRef.current; commit(); for (const item of clipboardRef.current) placedRef.current.set(key(lastTile.x + item.x, lastTile.y + item.y), item.value); if (mode === 'select') setSelection({ x: lastTile.x, y: lastTile.y, w, h }); bump(); setStatus('Pasted tile selection.') }
  const deleteSelection = () => { if (!selection) return setStatus('Use Select mode to choose tiles first.'); commit(); for (let y = selection.y; y < selection.y + selection.h; y++) for (let x = selection.x; x < selection.x + selection.w; x++) { placedRef.current.delete(key(x, y)); collisionsRef.current.delete(key(x, y)) } bump(); setStatus('Deleted selected tiles.') }
  const moveSelection = (dx, dy) => { if (!selection) return false; commit(); const moved = new Map(), movedCollisions = new Set(), inside = (x, y) => x >= selection.x && x < selection.x + selection.w && y >= selection.y && y < selection.y + selection.h; for (let y = selection.y; y < selection.y + selection.h; y++) for (let x = selection.x; x < selection.x + selection.w; x++) { const from = key(x, y), toX = x + dx, toY = y + dy, to = key(toX, toY), value = placedRef.current.get(from), hadCollision = collisionsRef.current.has(from); if (placedRef.current.has(to) && !inside(toX, toY)) { moved.set(from, value); continue } if (value) moved.set(to, value); placedRef.current.delete(from); collisionsRef.current.delete(from); if (hadCollision) movedCollisions.add(to) } for (const [k, v] of moved) if (v) placedRef.current.set(k, v); for (const k of movedCollisions) collisionsRef.current.add(k); setSelection(s => ({ ...s, x: s.x + dx, y: s.y + dy })); setLastTile(p => ({ x: p.x + dx, y: p.y + dy })); bump(); return true }
  const dropper = e => { const [x, y] = cell(e), placed = placedRef.current.get(key(x, y)), asset = placed?.asset || placed; if (!asset) return setStatus('No placed tile under the dropper.'); setSelected(asset); setSelectedRegion({ x: placed.sx ?? 0, y: placed.sy ?? 0, w: t, h: t }); setMode('brush'); setEraser(false); setStatus(`Picked ${asset.name} tile (${Math.floor((placed.sx ?? 0) / t)}, ${Math.floor((placed.sy ?? 0) / t)}).`) }
  const paint = e => { const [x, y] = cell(e), k = key(x, y); if (k === lastPaintRef.current) return; lastPaintRef.current = k; if (buttonRef.current === 2 || eraser) { placedRef.current.delete(k); collisionsRef.current.delete(k) } else if (collision) collisionsRef.current.add(k); else stamp(x, y); setLastTile({ x, y }); bump() }
  const onDown = e => { if (objectMode) return objectPointerDown(e); if (![0, 2].includes(e.button) || (mode === 'select' && e.button !== 0)) return; e.preventDefault(); setCursorActive(false); if (e.altKey && e.button === 0) { dropper(e); return } buttonRef.current = e.button; paintingRef.current = true; lastPaintRef.current = ''; e.currentTarget.setPointerCapture(e.pointerId); if (mode === 'select') { const [x, y] = cell(e); setLastTile({ x, y }); setSelectionStart({ x, y }); setSelection({ x, y, w: 1, h: 1 }) } else if (mode === 'eraser' || eraser || e.button === 2) { const [x, y] = cell(e); commit(); removeObjectAt(canvasPoint(e)); placedRef.current.delete(key(x, y)); collisionsRef.current.delete(key(x, y)); setLastTile({ x, y }); bump() } else { commit(); paint(e) } }
  const onMove = e => objectMode ? objectPointerMove(e) : paintingRef.current && (mode === 'select' ? updateSelection(e) : paint(e))
  const onUp = e => objectMode ? objectPointerUp(e) : (paintingRef.current = false, setSelectionStart(null), e.currentTarget.releasePointerCapture?.(e.pointerId))
  const onFiles = e => { const files = [...e.target.files]; if (!files.length) return; setSourceFile(files.at(-1)); files.forEach(file => addAsset(file, file.name, true, assetCategory)); setStatus(`Imported ${files.length} tile${files.length === 1 ? '' : 's'}.`) }
  const pickerCell = e => { const r = e.currentTarget.getBoundingClientRect(); return { x: Math.max(0, Math.min(Math.floor(selected.image.width / t) - 1, Math.floor(((e.clientX - r.left) / r.width) * selected.image.width / t))), y: Math.max(0, Math.min(Math.floor(selected.image.height / t) - 1, Math.floor(((e.clientY - r.top) / r.height) * selected.image.height / t))) } }
  const pickSubTile = e => { if (!selected) return; const p = pickerCell(e); setSelectedRegion({ x: p.x * t, y: p.y * t, w: t, h: t }); setStatus(`Selected ${selected.name} tile (${p.x}, ${p.y}).`) }
  const beginPicker = e => { if (!selected) return; e.preventDefault(); e.currentTarget.setPointerCapture?.(e.pointerId); const p = pickerCell(e); setPickerDragStart(p); setSelectedRegion({ x: p.x * t, y: p.y * t, w: t, h: t }) }
  const dragPicker = e => { if (!pickerDragStart) return; e.preventDefault(); const p = pickerCell(e), x = Math.min(p.x, pickerDragStart.x), y = Math.min(p.y, pickerDragStart.y), w = Math.abs(p.x - pickerDragStart.x) + 1, h = Math.abs(p.y - pickerDragStart.y) + 1; setSelectedRegion({ x: x * t, y: y * t, w: w * t, h: h * t }); setStatus(`Selected ${w}×${h} tile slice.`) }
  const endPicker = e => { e.currentTarget.releasePointerCapture?.(e.pointerId); setPickerDragStart(null) }
  const stamp = (x, y) => { if (!selected) return; for (let sy = 0; sy < selectedRegion.h; sy += t) for (let sx = 0; sx < selectedRegion.w; sx += t) { const dx = x + sx / t, dy = y + sy / t; if (dx >= 0 && dy >= 0 && dx < levelWidth && dy < 576 / t) placedRef.current.set(key(dx, dy), { asset: selected, sx: selectedRegion.x + sx, sy: selectedRegion.y + sy }) } }
  useEffect(() => { document.querySelectorAll('.picker-selection').forEach(el => { const scale = el.closest('.tileset-picker-large') ? modalPickerScale : pickerScale; el.style.width = `${selectedRegion.w * scale}px`; el.style.height = `${selectedRegion.h * scale}px` }) }, [selectedRegion, pickerScale, modalPickerScale])
  const fix = async () => { if (!sourceFile) return setStatus('Choose an image first.'); if (!['localhost', '127.0.0.1'].includes(window.location.hostname)) return setStatus('Pixel fixer is local-only. Run ./run.sh for this tool.'); setStatus('Fixing grid locally…'); const fd = new FormData(); fd.append('image', sourceFile); const response = await fetch('/api/fix', { method: 'POST', body: fd }); if (!response.ok) return setStatus(await response.text()); const blob = await response.blob(); setSourceFile(new File([blob], 'fixed.png', { type: 'image/png' })); addAsset(blob, 'fixed.png'); setStatus('Fixed asset imported.') }
  const eraseBackground = () => { if (!sourceFile) return setStatus('Choose an image first.'); const image = new Image(); image.onload = () => { const c = document.createElement('canvas'); c.width = image.width; c.height = image.height; const x = c.getContext('2d'); x.drawImage(image, 0, 0); const d = x.getImageData(0, 0, c.width, c.height), p = d.data, seen = new Uint8Array(c.width * c.height), seeds = [[0, 0], [c.width - 1, 0], [0, c.height - 1], [c.width - 1, c.height - 1]], same = (i, j) => Math.abs(p[i] - p[j]) + Math.abs(p[i + 1] - p[j + 1]) + Math.abs(p[i + 2] - p[j + 2]) < 72; for (const [sx, sy] of seeds) { const base = (sy * c.width + sx) * 4, q = [[sx, sy]]; while (q.length) { const [px, py] = q.pop(), n = py * c.width + px; if (px < 0 || py < 0 || px >= c.width || py >= c.height || seen[n]) continue; seen[n] = 1; const i = n * 4; if (!same(i, base)) continue; p[i + 3] = 0; q.push([px + 1, py], [px - 1, py], [px, py + 1], [px, py - 1]) } } x.putImageData(d, 0, 0); c.toBlob(blob => addAsset(blob, 'cutout.png'), 'image/png') }; image.src = URL.createObjectURL(sourceFile) }

  useEffect(() => { const keydown = e => { if (e.key === 'Control') { setCursor(lastTile); setCursorActive(true); return } if ((e.metaKey || e.ctrlKey) && e.key.toLowerCase() === 'z') { e.preventDefault(); undo(); return } if ((e.metaKey || e.ctrlKey) && e.key.toLowerCase() === 'y') { e.preventDefault(); redo(); return } if ((e.metaKey || e.ctrlKey) && ['ArrowLeft', 'ArrowRight', 'ArrowUp', 'ArrowDown'].includes(e.key)) { e.preventDefault(); if (selection) { moveSelection(e.key === 'ArrowLeft' ? -1 : e.key === 'ArrowRight' ? 1 : 0, e.key === 'ArrowUp' ? -1 : e.key === 'ArrowDown' ? 1 : 0); return } const maxX = levelWidth - 1, maxY = Math.floor(576 / t) - 1, next = { ...cursor }; if (e.key === 'ArrowLeft') next.x = Math.max(0, next.x - 1); if (e.key === 'ArrowRight') next.x = Math.min(maxX, next.x + 1); if (e.key === 'ArrowUp') next.y = Math.max(0, next.y - 1); if (e.key === 'ArrowDown') next.y = Math.min(maxY, next.y + 1); setCursor(next); setCursorActive(true); setStatus('Keyboard paint cursor active.'); commit(); const k = key(next.x, next.y); if (eraser) { placedRef.current.delete(k); collisionsRef.current.delete(k) } else if (collision) collisionsRef.current.add(k); else if (selected) placedRef.current.set(k, { asset: selected, sx: selectedRegion.x, sy: selectedRegion.y }); setLastTile(next); bump() } }; const keyup = e => { if (e.key === 'Control') setCursorActive(false) }; document.addEventListener('keydown', keydown); document.addEventListener('keyup', keyup); return () => { document.removeEventListener('keydown', keydown); document.removeEventListener('keyup', keyup) } }, [cursor, t, eraser, collision, selected, selectedRegion, lastTile, selection])

  useEffect(() => { const onKey = e => { const k = e.key.toLowerCase(), mod = e.metaKey || e.ctrlKey; if (k === 'f3') { e.preventDefault(); setDebug(v => !v); return } if (!mod && k === 'g') { switchLevel('gym'); return } if (!mod && k === 'l') { switchLevel('main'); return } if (pickerOpen && k === 'enter') { e.preventDefault(); setPickerOpen(false); setPickerDragStart(null); return } if (k === 'escape') { setPickerOpen(false); setPickerDragStart(null); setMode('brush'); setEraser(false); setSelection(null); return } if (mod && k === 'x') { e.preventDefault(); cutSelection() } else if (mod && k === 'c') { e.preventDefault(); copySelection() } else if (mod && k === 'v') { e.preventDefault(); pasteSelection() } else if ((k === 'delete' || k === 'backspace') && selection) { e.preventDefault(); deleteSelection() } else if (e.shiftKey && !mod && selection && ['arrowleft', 'arrowright', 'arrowup', 'arrowdown'].includes(k)) { e.preventDefault(); resizeSelection(k === 'arrowleft' ? -1 : k === 'arrowright' ? 1 : 0, k === 'arrowup' ? -1 : k === 'arrowdown' ? 1 : 0) } else if (!mod && k === 'b') { setMode('brush'); setEraser(false); setStatus('Brush mode.') } else if (!mod && k === 'e') { setMode('eraser'); setEraser(true); setStatus('Eraser mode.') } else if (!mod && k === 'm') { setMode('select'); setEraser(false); setStatus('Select mode: drag a tile rectangle.') } }; document.addEventListener('keydown', onKey); return () => document.removeEventListener('keydown', onKey) }, [selection, lastTile, pickerOpen])
  useEffect(() => { const onKey = e => { if (!selected || !e.altKey || !['ArrowLeft', 'ArrowRight', 'ArrowUp', 'ArrowDown'].includes(e.key)) return; e.preventDefault(); const dw = e.key === 'ArrowRight' ? t : e.key === 'ArrowLeft' ? -t : 0, dh = e.key === 'ArrowDown' ? t : e.key === 'ArrowUp' ? -t : 0; setSelectedRegion(r => ({ ...r, w: Math.max(t, r.w + dw), h: Math.max(t, r.h + dh) })); setStatus('Picker slice resized with Option + arrows.') }; document.addEventListener('keydown', onKey); return () => document.removeEventListener('keydown', onKey) }, [selected, t])

  const deleteAsset = asset => { commit(); for (const [k, v] of placedRef.current) if ((v.asset || v) === asset) placedRef.current.delete(k); assetsRef.current = assetsRef.current.filter(a => a !== asset); setAssets(assetsRef.current); removePersisted(asset.name); if (selected === asset) select(null); bump(); setStatus(`Deleted ${asset.name}.`) }
  const download = (name, text) => { const a = document.createElement('a'); a.href = URL.createObjectURL(new Blob([text], { type: 'text/plain' })); a.download = name; a.click() }
  const exportLevelPng = () => { const canvas = canvasRef.current; if (!canvas) return; canvas.toBlob(blob => { if (!blob) return; const link = document.createElement('a'); link.href = URL.createObjectURL(blob); link.download = `${level}-level.png`; link.click(); setStatus(`Downloaded ${level}-level.png.`) }, 'image/png') }
  const exportLevelJson = () => { download(currentDoc.metadata.id + '.level.json', JSON.stringify(currentDoc, null, 2)); setStatus('Downloaded structured level data.') }
  const exportScene = name => { const used = [...new Set([...placedRef.current.values()].map(v => v.asset || v))], ids = new Map(used.map((a, i) => [a, `Texture_${i + 1}`])), lines = [`[gd_scene load_steps=${used.length + 2} format=3]`]; for (const a of used) lines.push('', `[ext_resource type="Texture2D" path="res://art/${a.name}" id="${ids.get(a)}"]`); lines.push('', '[sub_resource type="RectangleShape2D" id="RectangleShape2D_1"]', `size = Vector2(${t}, ${t})`, '', '[node name="Level" type="Node2D"]', 'texture_filter = 1'); let i = 0; for (const [k, placed] of placedRef.current) { const a = placed.asset || placed, sx = placed.sx ?? 0, sy = placed.sy ?? 0, [x, y] = k.split(',').map(Number); lines.push(`\n[node name="${a.name.replace(/\W/g, '_')}_${i++}" type="Sprite2D" parent="."]`, `texture = ExtResource("${ids.get(a)}")`, `region_enabled = true`, `region_rect = Rect2(${sx}, ${sy}, ${t}, ${t})`, `position = Vector2(${x * t + t / 2}, ${y * t + t / 2})`) } for (const k of collisionsRef.current) { const [x, y] = k.split(',').map(Number); lines.push(`\n[node name="Collision_${x}_${y}" type="StaticBody2D" parent="."]`, `position = Vector2(${x * t + t / 2}, ${y * t + t / 2})`, `[node name="CollisionShape2D" type="CollisionShape2D" parent="Collision_${x}_${y}"]`, 'shape = SubResource("RectangleShape2D_1")') } download(name, lines.join('\n')); setStatus(`Downloaded ${name}.`) }

  return <><header><h1>Terrain + Props Editor</h1><span>{status}</span></header><main>
    <aside><h2>Props</h2><div className="eyebrow">Asset brush</div><small>Choose a tile, then drag on the canvas.</small><div className="eyebrow">Import</div><select value={assetCategory} onChange={e => setAssetCategory(e.target.value)} aria-label="Asset category">{CATEGORIES.map(category => <option key={category} value={category}>{category}</option>)}</select><input type="file" accept="image/png,image/jpeg" multiple onChange={onFiles}/><small>Uploaded tiles stay in this browser.</small><div className="eyebrow">Prop catalog</div>{CATEGORIES.map(category => { const group = assets.filter(asset => (asset.category || "terrain") === category); return group.length ? <div className="catalog-group" key={category}><div className="catalog-heading">{category}</div><div id="assetList">{group.map(asset => <div className={`asset ${selected === asset ? "selected" : ""}`} key={asset.name} onClick={() => select(asset)}><img src={asset.url} alt={asset.name}/><span className="asset-name">{asset.name}</span><button className="asset-delete" title={`Delete ${asset.name}`} aria-label={`Delete ${asset.name}`} onClick={e => { e.stopPropagation(); deleteAsset(asset) }}>×</button></div>)}</div></div> : null})}</aside>
    <section id="stage">{debug && <div className="debug-panel"><strong>DEBUG</strong><span>tiles {placedRef.current.size}</span><span>collisions {collisionsRef.current.size}</span><span>mode {mode}</span><span>level {currentDoc.metadata.name}</span></div>}<div className="canvas-wrap"><div className="canvas-tools" aria-label="Selection tools"><button className={mode === 'brush' ? 'active' : ''} title="Brush (B)" aria-label="Brush (B)" onClick={() => { setMode('brush'); setEraser(false) }}><MousePointer2 size={16}/></button><button className={mode === 'select' ? 'active' : ''} title="Rectangular select (M)" aria-label="Rectangular select (M)" onClick={() => { setMode('select'); setEraser(false) }}><BoxSelect size={16}/></button><button className={mode === 'eraser' ? 'active' : ''} title="Eraser (E)" aria-label="Eraser (E)" onClick={() => { setMode('eraser'); setEraser(true) }}><Eraser size={16}/></button><button title="Copy selection (Ctrl/⌘ C)" aria-label="Copy selection" onClick={copySelection}><Copy size={16}/></button><button title="Paste at last tile (Ctrl/⌘ V)" aria-label="Paste selection" onClick={pasteSelection}><ClipboardPaste size={16}/></button></div><div className="axis-x">{Array.from({ length: levelWidth }, (_, i) => <span key={i} style={{ left: `${i * t * zoom + 3}px` }}>{i}</span>)}</div><div className="axis-y">{Array.from({ length: Math.floor(576 / t) }, (_, i) => <span key={i} style={{ top: `${i * t * zoom + 3}px` }}>{i}</span>)}</div><canvas ref={canvasRef} width={levelWidth * t} height="576" style={{ width: `${levelWidth * t * zoom}px`, height: `${576 * zoom}px` }} onContextMenu={e => e.preventDefault()} onPointerDown={onDown} onPointerMove={onMove} onPointerUp={onUp} onPointerCancel={onUp}/></div></section>
    <aside className="terrain-sidebar"><div className="terrain-action-rail" aria-label="Editor actions"><span className="rail-label">LEVEL</span><button title="Create new level" aria-label="Create new level" onClick={() => { const id = prompt('Level id', 'level-' + (Object.keys(levelDocs).length + 1)); if (!id) return; const docs = { ...levelDocs, [id]: newLevelDoc(id, id) }; persistLevels(docs); switchLevel(id) }}><Plus size={16}/></button><button title="Save current level" aria-label="Save current level" onClick={() => { persistLevels(levelDocs); setStatus('Saved ' + currentDoc.metadata.name + '.') }}><Save size={16}/></button><button title="Save as new level" aria-label="Save as new level" onClick={() => { const id = prompt('Save level as', currentDoc.metadata.id + '-copy'); if (!id) return; persistLevels({ ...levelDocs, [id]: { ...currentDoc, metadata: { ...currentDoc.metadata, id, name: id } } }); setLevel(id); setStatus('Saved as ' + id + '.') }}><FilePlus2 size={16}/></button><span className="rail-label">OBJECTS</span><button className={objectMode ? 'active' : ''} title="Object select mode" aria-label="Object select mode" onClick={() => setObjectMode(v => !v)}><Crosshair size={16}/></button>{OBJECT_TYPES.map(type => <button key={type} title={'Add ' + type} aria-label={'Add ' + type} onClick={() => addObject(type)}>{type === 'platform' ? <Square size={15}/> : type === 'prop' ? <Box size={15}/> : type === 'pickup' ? <Circle size={15}/> : type === 'enemy' ? <Circle size={15}/> : <DoorOpen size={15}/>}</button>)}<span className="rail-label">VIEW</span><button title="Fix pixel grid" aria-label="Fix pixel grid" onClick={fix}><WandSparkles size={16}/></button><button title="Remove corner background" aria-label="Remove corner background" onClick={eraseBackground}><Circle size={16}/></button><button title="Zoom out" aria-label="Zoom out" onClick={() => setZoom(z => Math.max(.5, +(z - .25).toFixed(2)))}><ZoomOut size={16}/></button><button title="Reset zoom" aria-label="Reset zoom" onClick={() => setZoom(1)}><RotateCcw size={14}/></button><button title="Zoom in" aria-label="Zoom in" onClick={() => setZoom(z => Math.min(3, +(z + .25).toFixed(2)))}><ZoomIn size={16}/></button><span className="rail-label">EDIT</span><button className={mode === 'brush' ? 'active' : ''} title="Brush (B)" aria-label="Brush (B)" onClick={() => { setMode('brush'); setEraser(false) }}><MousePointer2 size={16}/></button><button className={mode === 'select' ? 'active' : ''} title="Rectangular select (M)" aria-label="Rectangular select (M)" onClick={() => { setMode('select'); setEraser(false) }}><BoxSelect size={16}/></button><button className={mode === 'eraser' ? 'active' : ''} title="Eraser (E)" aria-label="Eraser (E)" onClick={() => { setMode('eraser'); setEraser(true) }}><Eraser size={16}/></button><button title="Copy selection (Ctrl/⌘ C)" aria-label="Copy selection" onClick={copySelection}><Copy size={16}/></button><button title="Paste selection (Ctrl/⌘ V)" aria-label="Paste selection" onClick={pasteSelection}><ClipboardPaste size={16}/></button><button title="Import image" aria-label="Import image" onClick={() => document.querySelector('input[type=file]').click()}><Upload size={16}/></button><button title="Save scene" aria-label="Save scene" onClick={() => exportScene('level.tscn')}><Save size={16}/></button><button title="Save scene as" aria-label="Save scene as" onClick={() => { const n = prompt('Scene filename', 'level.tscn'); if (n) exportScene(n.endsWith('.tscn') ? n : n + '.tscn') }}><FilePlus2 size={16}/></button><button title="Undo" aria-label="Undo" onClick={undo}><RotateCcw size={16}/></button><button title="Redo" aria-label="Redo" onClick={redo}><RotateCw size={16}/></button><button title="Clear level" aria-label="Clear level" onClick={() => { if (!placedRef.current.size && !collisionsRef.current.size) return; commit(); placedRef.current.clear(); collisionsRef.current.clear(); bump() }}><Trash2 size={16}/></button><span className="rail-label">EXPORT</span><button title="Export level PNG" aria-label="Export level PNG" onClick={exportLevelPng}><Download size={16}/></button><button title="Export structured level JSON" aria-label="Export structured level JSON" onClick={exportLevelJson}><FileJson size={16}/></button><button title="Download assets" aria-label="Download assets" onClick={() => assets.forEach(a => { const link = document.createElement('a'); link.href = a.url; link.download = a.name; link.click() })}><Download size={16}/></button><button title="Export Godot scene" aria-label="Export Godot scene" onClick={() => exportScene('level.tscn')}><Box size={16}/></button></div><h2>Terrain</h2><div className="eyebrow">Level file</div><select value={level} onChange={e => switchLevel(e.target.value)} aria-label="Load level">{Object.values(levelDocs).map(doc => <option key={doc.metadata.id} value={doc.metadata.id}>{doc.metadata.name}</option>)}</select><label>Width (tiles)<input type="number" min="24" value={levelWidth} onChange={e => updateCurrentDoc(doc => ({ ...doc, metadata: { ...doc.metadata, width: Math.max(24, Number(e.target.value) || 24) } }))}/></label><div className="tool-strip"><button title="Create new level" onClick={() => { const id = prompt('Level id', 'level-' + (Object.keys(levelDocs).length + 1)); if (!id) return; const docs = { ...levelDocs, [id]: newLevelDoc(id, id) }; persistLevels(docs); switchLevel(id) }}><Plus size={16}/></button><button title="Save current level" onClick={() => { persistLevels(levelDocs); setStatus('Saved ' + currentDoc.metadata.name + '.') }}><Save size={16}/></button><button title="Save as new level" onClick={() => { const id = prompt('Save level as', currentDoc.metadata.id + '-copy'); if (!id) return; persistLevels({ ...levelDocs, [id]: { ...currentDoc, metadata: { ...currentDoc.metadata, id, name: id } } }); setLevel(id); setStatus('Saved as ' + id + '.') }}><FilePlus2 size={16}/></button></div><div className="eyebrow">Objects</div><div className="tool-strip object-tools">{OBJECT_TYPES.map(type => <button key={type} title={'Add ' + type} onClick={() => addObject(type)}>{type === 'platform' ? <Square size={15}/> : type === 'prop' ? <Box size={15}/> : type === 'pickup' ? <Circle size={15}/> : type === 'enemy' ? <Circle size={15}/> : <DoorOpen size={15}/>}</button>)}<button className={objectMode ? 'active' : ''} title="Object select mode" onClick={() => setObjectMode(v => !v)}><Crosshair size={16}/></button></div>{selectedObject && <div className="object-inspector"><strong>{objectSelection.type}</strong><label>X<input type="number" value={Math.round(selectedObject.x)} onChange={e => updateSelectedObject({ x: Number(e.target.value) || 0 })}/></label><label>Y<input type="number" value={Math.round(selectedObject.y)} onChange={e => updateSelectedObject({ y: Number(e.target.value) || 0 })}/></label>{objectSelection.type === 'platform' && <><label>Asset ID<input value={selectedObject.assetId} onChange={e => updateSelectedObject({ assetId: e.target.value })}/></label><label>Scale<input type="number" step="0.1" value={selectedObject.scale || 1} onChange={e => updateSelectedObject({ scale: Number(e.target.value) || 1 })}/></label><label><input type="checkbox" checked={!!selectedObject.collision} onChange={e => updateSelectedObject({ collision: e.target.checked })}/> collision</label></>}{objectSelection.type === 'prop' && <label>Frame ID<input value={selectedObject.frameId} onChange={e => updateSelectedObject({ frameId: e.target.value })}/></label>}{objectSelection.type === 'pickup' && <label>Pickup type<input value={selectedObject.pickupType} onChange={e => updateSelectedObject({ pickupType: e.target.value })}/></label>}{objectSelection.type === 'enemy' && <><label>Enemy ID<input value={selectedObject.enemyId} onChange={e => updateSelectedObject({ enemyId: e.target.value })}/></label><label>Facing<select value={selectedObject.facing} onChange={e => updateSelectedObject({ facing: e.target.value })}><option>left</option><option>right</option></select></label></>}{objectSelection.type === 'exit' && <><label>Exit ID<input value={selectedObject.exitId} onChange={e => updateSelectedObject({ exitId: e.target.value })}/></label><label>Target animation<input value={selectedObject.targetAnimation} onChange={e => updateSelectedObject({ targetAnimation: e.target.value })}/></label></>}<button className="danger" onClick={deleteSelectedObject}>Delete object</button></div>}<div className="eyebrow legacy-action-label">Level</div><div className="level-tabs"><button className={level === 'main' ? 'active' : ''} onClick={() => switchLevel('main')}>Main Level</button><button className={level === 'gym' ? 'active' : ''} onClick={() => switchLevel('gym')}>Gym</button></div><div className="eyebrow legacy-action-label">Image tools</div><div className="tool-strip"><button className="primary" title="Fix pixel grid" aria-label="Fix pixel grid" onClick={fix}><WandSparkles size={16}/></button><button className="secondary" title="Remove corner background" aria-label="Remove corner background" onClick={eraseBackground}><Circle size={16}/></button></div><div className="eyebrow legacy-action-label">Zoom</div><div className="tool-strip"><button title="Zoom out" aria-label="Zoom out" onClick={() => setZoom(z => Math.max(.5, +(z - .25).toFixed(2)))}><ZoomOut size={16}/></button><button title="Reset zoom" aria-label="Reset zoom" onClick={() => setZoom(1)}><RotateCcw size={14}/>{Math.round(zoom * 100)}%</button><button title="Zoom in" aria-label="Zoom in" onClick={() => setZoom(z => Math.min(3, +(z + .25).toFixed(2)))}><ZoomIn size={16}/></button></div><div className="eyebrow legacy-action-label">Actions</div><div className="tool-strip"><button title="Import image" aria-label="Import image" onClick={() => document.querySelector('input[type=file]').click()}><Upload size={16}/></button><button title="Save scene" aria-label="Save scene" onClick={() => exportScene('level.tscn')}><Save size={16}/></button><button title="Save scene as" aria-label="Save scene as" onClick={() => { const n = prompt('Scene filename', 'level.tscn'); if (n) exportScene(n.endsWith('.tscn') ? n : `${n}.tscn`) }}><FilePlus2 size={16}/></button><button className="secondary" title="Undo (Ctrl/⌘ Z)" aria-label="Undo" onClick={undo}><RotateCcw size={16}/></button><button className="secondary" title="Redo (Ctrl/⌘ Y)" aria-label="Redo" onClick={redo}><RotateCw size={16}/></button><button className="secondary" title="Clear level" aria-label="Clear level" onClick={() => { if (!placedRef.current.size && !collisionsRef.current.size) return; commit(); placedRef.current.clear(); collisionsRef.current.clear(); bump() }}><Trash2 size={16}/></button></div><div className="eyebrow">Selected tile</div><div className="selection"><img src={selected?.url || ''} alt="No tile selected"/><strong>{selected?.name || 'Nothing selected'}</strong></div><div className="eyebrow">Tileset picker</div><small>Click to choose a tile. Use the large picker to drag a rectangular slice.</small>{selected && <><div className="tileset-picker"><img src={selected.url} alt={`${selected.name} tileset`} onLoad={e => setPickerScale(e.currentTarget.clientWidth / e.currentTarget.naturalWidth)} onClick={pickSubTile} draggable={false} onDragStart={e => e.preventDefault()}/><div className="picker-selection" style={{ left: `${selectedRegion.x * pickerScale}px`, top: `${selectedRegion.y * pickerScale}px`, width: `${t * pickerScale}px`, height: `${t * pickerScale}px` }}/></div><button className="picker-open" onClick={() => setPickerOpen(true)}><Box size={15}/> Open picker</button></>}{selected && pickerOpen && <div className="picker-modal" role="dialog" aria-modal="true" aria-label="Tileset picker"><div className="picker-modal-card"><div className="picker-modal-head"><strong>{selected.name}</strong><button onClick={() => setPickerOpen(false)} aria-label="Close tileset picker">×</button></div><small>Drag a rectangular slice. Tile size: {t}px.</small><div className="tileset-picker tileset-picker-large"><img src={selected.url} alt={`${selected.name} enlarged tileset`} onLoad={e => setModalPickerScale(e.currentTarget.clientWidth / e.currentTarget.naturalWidth)} onPointerDown={beginPicker} onPointerMove={dragPicker} onPointerUp={endPicker} draggable={false} onDragStart={e => e.preventDefault()}/><div className="picker-selection" style={{ left: `${selectedRegion.x * modalPickerScale}px`, top: `${selectedRegion.y * modalPickerScale}px`, width: `${t * modalPickerScale}px`, height: `${t * modalPickerScale}px` }}/></div><button className="primary" onClick={() => setPickerOpen(false)}>Done</button></div></div>}<label>Tile size<input type="number" value={tileSize} min="8" step="8" onChange={e => setTileSize(e.target.value)}/></label><label>Godot asset folder<input value="res://art/" readOnly/></label><div className="row"><input type="checkbox" checked={collision} onChange={e => setCollision(e.target.checked)}/><label>paint collision</label></div><div className="row"><input type="checkbox" checked={grid} onChange={e => setGrid(e.target.checked)}/><label>grid</label></div><div className="row"><input type="checkbox" checked={debug} onChange={e => setDebug(e.target.checked)}/><label>debug</label></div><div className="eyebrow legacy-action-label">Export</div><div className="tool-strip"><button className="secondary" title="Export level PNG" aria-label="Export level PNG" onClick={exportLevelPng}><Download size={16}/></button><button className="secondary" title="Export structured level JSON" aria-label="Export structured level JSON" onClick={exportLevelJson}><FileJson size={16}/></button><button className="secondary" title="Download assets" aria-label="Download assets" onClick={() => assets.forEach(a => { const link = document.createElement('a'); link.href = a.url; link.download = a.name; link.click() })}><Download size={16}/></button><button className="primary" title="Export Godot scene" aria-label="Export Godot scene" onClick={() => exportScene('level.tscn')}><Box size={16}/></button></div></aside>
  </main></>
}

createRoot(document.getElementById('root')).render(<App />)
