diff --git a/rust/src/main.rs b/rust/src/main.rs
index b7daccb..5ac2d2b 100644
--- a/rust/src/main.rs
+++ b/rust/src/main.rs
@@ -3,2 +3,3 @@
 /// Peak working-set size (bytes) of this process, via psapi.
+#[cfg(target_os = "windows")]
 fn peak_rss() -> u64 {
@@ -34 +35,6 @@
+// The upstream CLI reports this Windows-only diagnostic. It is not part of
+// reconstruction, so keep the command portable rather than adding an OS API.
+#[cfg(not(target_os = "windows"))]
+fn peak_rss() -> u64 { 0 }
+
 fn main() {
